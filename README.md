# TaMP
Дата: 03.04.2026
СУБД: SQLiteStudio
Выполнено: Тестирование модуля регистрации, авторизации и хеширования паролей
Добавлен класс db_handler (файлы DB_handler.h, DB_handler.cpp) — инкапсулирует работу с SQLite3
Добавлен метод Tables() — создаёт таблицу users_test при инициализации, вызывается в конструкторе
Добавлен метод reg_user() — регистрация пользователя (login, hash, email) через параметризованный INSERT
Добавлен метод auth_user() — авторизация по login + hash через SELECT
Добавлен модуль md5_util.h — реализация MD5 без внешних зависимостей
Добавлен метод hashPassword() — статический, хеширует пароль перед сохранением и проверкой
Исправлен баг: Tables() не вызывался в конструкторе — таблица не создавалась, INSERT падал
Протестирована регистрация и авторизация в main.cpp
